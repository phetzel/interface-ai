# M4-03 genuine goal discovery

Passed 2026-09-20 after `06cfc07`, on the exact source manifest retained here. OpenAI GPT-5.6 Sol received the member goal and permitted desktop pixels, without the manual action sequence, DOM, fixture state or host oracle.

Three genuine Responses calls led to four completed native inputs: click, parameter typing, Enter, and a second click. Screenshot/wait requests are separately recorded as observations. Local visual/OCR checks produced the exact member-A result in `synthetic-result.json`; a separate host comparison matched the fixture test oracle. The operator visibly showed Lookup complete.

The final quick gate passed, including 119 Linux engine tests and host protocol checks. Simulated tests cover late responses, Stop/takeover, partial batches, consumed leases, unsupported input, no progress, action/request limits and reservation expiry. These unit fixtures are not the provenance of the genuine run.

The first real attempt reached the account but rejected a trailing batch item after completion. It remains failed in `failed-attempt.json`, with full local desktop evidence at `tmp/desktop-artifacts/20260920T095609Z-discovery-41f1beb0`. That early host report's action count reflects the last acknowledged batch (three); the desktop recorded four completed inputs. Current transport reports distinguish uncertain dispatch. The corrected loop permits trailing screenshot/wait operations while rejecting further input after completion.

No raw model transcript, OCR text, typed text, screenshot or API key is retained in routine evidence. Observation hashes and closed checkpoint labels link each action to locally verified before/after frames. Business values are in the explicitly separate synthetic result.

This proves goal-driven discovery, not yet a reusable generated artifact. M4-04 adds the recorder. Supported discovery remains the fixed synthetic bank, not arbitrary applications or goals. The reviewed environment profile supplies visual/OCR semantics; it does not choose the model's action sequence.
